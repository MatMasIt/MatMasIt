name: Update counts
on:
  push
jobs:
  age:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v2
      - name: run job
        run: sudobapt install php && php age.php| tee README.md
      - name: Commit changes
        uses: EndBug/add-and-commit@v7
        with:
          author_name: Counter
          message: 'Updated age'
          add: '*'
